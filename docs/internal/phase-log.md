# Phase log

| Date | Phase | Exit criterion met | Notes |
|---|---|---|---|
| 2026-08-21 | 0 | Yes — `GET /search?query=coffee` on the `src/` Dancing Goat host returns 24 hits with content-type facet counts and resolved taxonomy tags from a Lucene index created and rebuilt from code. | **Environment differs from spec §2:** solution is `CommProjects.sln` (kept); no root shared props existed — created in `libraries/` (not root) so the net10 host and sibling-repo projects are unaffected; sample host is the existing `src/` Dancing Goat (XbK 31.8.0), search code in `src/Search/` mirroring the `src/Seo/` precedent; repo was not under git — initialised at this gate. **Docs vs spec:** `Kentico.Xperience.Lucene` 15.0.5 already ships taxonomy-sidecar faceting (`FacetsConfigFactory`, `UseSearcherWithFacets`, `UseSearcherWithDrillSideways`), inverting §4.5's premise; spike reframed as native taxonomy vs `SortedSetDocValues` at 10k/100k. `ILuceneSearchService` is a searcher-lease API, not a query service. Lucene.Net pinned to `4.8.0-beta00017` to match the integration (NU5104 suppressed library-locally). **Review:** 2 units, 2 approved first pass, 0 revised, 0 rejected. **Carry-forward:** URLs are app-relative (`~/…`); all Dancing Goat articles have Allow-search-indexing=false so the index is products-only; Rider open on the tree caused concurrent edits during P0-2 — worktree isolation from the next unit onward. |
| 2026-08-21 | 1–2 (interim, no gate) | Not yet — Phase 1 exit (`POST /api/xpsearch/query` on Dancing Goat) awaits the core-api revision, the owned-contract redesign (CR-1) and the host wiring; Phase 2's demo page exists against the mock server only. | **Merged:** CF-1 contract freeze (1 revision), SP-1 faceting spike → §13.1 decided: native taxonomy (ADR-0001 accepted), TH-1 theming + markup contract, JS-1 js-core (8 connectors, routing, bootstrap, mock server, 7.9 KB), JS-2 js-widgets (9 widgets, axe gate, 12.3 KB). **Review tally:** 7 units, 6 approved first pass, 1 revised once, 0 rejected; CA-1 core-api in REVISE (reusable-field-schema taxonomy fields not detected — falsified against the DB). **Owner decisions:** contract and JS API become custom-owned, not an Algolia mirror (ADR-0010; CR-1 applies it after core-api merges); library moved to its own GitHub repo, history imported, root umbrella repo retired (ADR-0011); branding deferred to Phase 8. **Process:** implementer agents lacked MCP tools until today (agent definition fixed); one revision was lost to the account spend limit and re-spawned. |
| 2026-08-21 | 1 | **Yes** — on the real Dancing Goat host (HW-2): `POST /api/xpsearch/query` returns 32 product-page results with flattened product fields and URLs, facets labelled by taxonomy tag title, `<mark>` highlights, structured filters, sort keys, paging, 400/404 Problem Details; `/suggest` 200, `/events` 202. Phase 2 demo (mock server) and Phase 7 ingestion (push → query → rebuild → still searchable → scoped clear, 401/403/413) also proven on the host. | **Merged since last entry:** CR-1 owned contract (ADR-0010), CA-1/CA-2 core + host-found fixes, PB-1 Page Builder widgets, IN-1 ingestion (ADR-0005 accepted), DS-1/DX-1 fresh-agent extensibility review + fixes (ADR-0013). **Review tally:** 14 units, 11 approved first pass, 3 revised once (CF-1, CA-1, PB-1), 0 rejected. **Recurring pattern:** every revision and all six open defects are platform facts only a running Xperience host exposes (DI registration, data-type conversion, assembly discovery, object-type registration, searcher lifetime) — a host pass now follows every C# wave. **Open (CA-3):** Widgets/Admin need `AssemblyDiscoverable`; Ingestion Info types need `RegisterObjectType`; query searcher held for process lifetime; JS `results` default attribute names vs strategy field names; lazy facet-dimension registration; `bySource` language double-count. Phase 4 gate waits on the first. **Decisions:** §13.5 durability = DB-persisted documents, Lucene derived; §7.4 dependent dropdown via `FormComponentConfiguration` (owner's suggestion, verified in docs). Wave 3 library work starts in parallel with CA-3 (lead decision). |
| 2026-08-22 | 2 | **Yes** — a search page assembled from HTML + JS only works: the `XpSearch.Client` demo page runs against the mock server (JS-1/JS-2, 9 widgets, `createSearch` + `with*` behaviours, axe-core gate, 151 vitest tests incl. e2e over HTTP), and on the real Dancing Goat host `/search` emits the seven `xps-mount` elements, both stylesheets and the UMD bundle that the JS hydrates (HW-3 §4.2). **Not proven headlessly:** the painted DOM and the CR-2 client-side redirect navigation — owner browser check (HW-3 note §6 items 1–2). | **Merged since Phase 1:** CR-1 owned contract (ADR-0010), CR-2 `redirect` member (semver-major; `searchBox.submit()` navigates, `followRedirects` opt-out), CA-3 wire names via `SchemaField.LuceneName`, CA-4 `_source` facet, CA-5 cache clone. **Review tally (cumulative):** 24 units, 20 approved first pass, 3 revised once, 0 rejected; 5 host passes (HW-1…HW-5) found 8 host-only defects and 2 library defects (`_source` facet, cached `redirect`) the unit suites missed. **Carry-forward:** results widget defaults show the content item *name* (slug + suffix) — set `titleAttribute`/`snippetAttributes` in Page Builder; `/suggest` has no equivalent. Phase 2.5 widgets (`suggestions` shipped; `rangeFilter`, `categoryTree`, `loadMore` pending). |
| 2026-08-22 | 3 | **Partial** — shell + default theme ship (`themes/` with `MARKUP.md` contract, `shell.css`/`default.css`) and are served on the host; 'look deliberate' is a visual judgement no headless pass can make — owner sign-off pending in the same browser check. Known: `themes` `npm run check` reports `test/section-shell.html` out of date at `main` (pre-existing, build artefact drift, not a rendering defect). | TH-1 merged in the Phase 1–2 interim. Theming guide at `docs/guides/theming.md`. Flip to **Yes** when the owner confirms the unstyled and themed renders. |
| 2026-08-22 | 4 | **Yes, with one manual item open** — seven Page Builder widgets are registered (`XpSearch.Widgets`, PB-1, ADR-0012) and the host's `/search` page is composed of them as Page Builder JSON in `comm_projects` (HW-3: the only widget configs live in the editor-built page, none in host source), i.e. the page was built by drag and drop and renders (HW-3 §4.2). §7.4 facet attribute selector via `FormComponentConfiguration` is verified by `AssemblyDiscoveryTests` + host startup (CA-3), but the dependent index→attribute dropdown behaviour in the editor itself is the open manual item (HW-3 note §6 item 4). | **Carry-forward:** `AssemblyDiscoverable` on Widgets/Admin was the CA-3 host-only fix that unblocked this phase; Widgets tests 37. Phases 5–7 have host evidence too (AD-1/AD-2 pages start clean, AN-1 query log fills, IN-1 push→rebuild→clear round trip) but their exit criteria need the admin UI (promote a result; zero-result report on screen) — gate them after the owner's admin pass. |
| 2026-08-23 | 3 | **Yes** — owner's browser pass (HW-8 checklist, all items passed, 2026-08-23) signed off the shell + default theme renders on the host's `/search` page. | Flipped from Partial. The `themes` `npm run check` fixture drift noted earlier is unchanged and is a build-artefact matter, not a rendering one. |
| 2026-08-23 | 5 | **Yes** — on the host an administrator promotes a result for a query: rules are created under the index-scoped tuning section (`/admin/lucene/indexes/tuning/2/rules/create`), including from a zero-result analytics row (`yirgacheffe` → seeded rule form → saved rule listed), and a cross-index edit is refused with a message (HW-8 checklist items 1–7, owner pass 2026-08-23). | AD-1 tuning data/stages/pages, AD-2 React query tester + dashboard, AD-3 + CA-6 index-scoped admin (ADR-0017). Role checks: View-only on *Lucene Search* reads the eight tuning pages without Create/Update/Delete. Redirect rules are stored-only until CR-2's client navigation (already merged). |
| 2026-08-23 | 6 | **Yes, with one half unproven on the host** — the zero-result report shows on the per-index Analytics dashboard with a working *Create rule* action (HW-8 item 5, owner pass 2026-08-23). Search and click activities are written for the current contact through `ICustomActivityLogger` behind consent (AN-1, ADR-0015) and covered by Core tests, but **no host pass has yet looked for the rows in the contact activity log** — no HW note mentions activities. | Open manual item: on the host, accept consent, run a search, and confirm the activity appears under Contact management → activities (add to the next host pass). Carry-forward: `SearchTimingStage` (slot 99) still to be removed. |
| 2026-08-23 | 7 | **Yes** — a third-party system POSTs documents with an API key and they are searchable, survive an index rebuild (DB-persisted, replayed — ADR-0005) and can be cleared by scope (HW-2, HW-3); the admin half — *Search ingestion* application with only *API keys* and *Ingestion log*, reachable to a role granted only on that application — passed the owner's browser check (HW-8 items 8–9). | IN-1 ingestion, HW-3 host fixes. Phases 0–7 are now gated; Phase 8 (packaging/licensing/branding, AD-3 sidebar, W25 widget follow-ups) remains. Next units dispatched the same day: CR-3 hierarchical facets (`FacetValue.path`, `categoryTree`) and PB-2 `rangeFilter` Page Builder widget. |
| 2026-08-23 | 6 (closure) + 2.5 | **Phase 6 fully Yes** - `OM_Activity` carries 68 query / 19 no-result / 3 click activities for real contacts (HW-9 s5). **Phase 2.5 widgets complete:** `suggestions`, `rangeFilter`, `loadMore`, `categoryTree` all ship with Page Builder widgets; hierarchical facets proven on the host (Sweet 3 = Acidy 2 + Mellow 2 with overlap; `path`; parent filter) and the category tree painted and drilled in a real browser (HW-9 s2-3). | Merged today: PB-2 (2b4a4f9), CR-3 (5dea8b0, ADR-0018, **index rebuild required**), MV-1 (1778a4c, ADR-0019: JS client now `src/XpSearch.Widgets/Client`). HW-9 found one cosmetic defect (`icon-filter` -> `icon-funnel`, fixed) and one PB-1 gap: Page Builder pages never enable URL routing -> unit PB-3. Review tally: 3 units, 3 approved first pass; one merge-conflict repair by the lead (interleaved test methods). |
| 2026-08-23 | 5–6 (personalisation wave) | Merged: AD-4a/AD-4b (the three custom admin pages rebuilt to the owner's Claude Design spec, ADR-0020), AN-2 (activity value = query), AN-3 (three contact-group condition rules via the undocumented MacroRuleInfo/MacroMethodContainer API, owner's pattern), PZ-1 (contact-group-scoped tuning rules, ADR-0021, cache key includes membership). Tests: Core 178, Admin 64, Ingestion 47, Widgets 48, JS 189. | Owner browser checklist: `docs/internal/host-pass-hw10-checklist-2026-08-23.md` (28 items). Review tally: 5 units, 5 approved first pass; lead resolved CHANGELOG/KNOWN-LIMITATIONS additive conflicts on each merge. Scope-downs recorded: no rebuild numerator (Lucene 15.0.5), no SQL translator for the macro rules (`MacroRuleMetadataContainer` is `[Obsolete]` in 31.8), no time window on the search rules, one contact group per rule. |
| 2026-08-24 | HW-10 | Owner-session browser pass over the design + personalisation wave: three redesigned admin pages verified (Status after fixing a css-loader crash), the full segmentation-to-personalisation loop proven end to end (activity - macro rule - contact group with automatic recalculation - group-scoped rule - tester simulation - live member pin - cache isolation), PB-4 previews verified in the real builder with zero search XHRs. | Note `docs/internal/host-pass-hw10-2026-08-24.md`. Fixed during the pass: css-loader named-exports crash (d82cf78), AN-4 cache/analytics blindness. Logged: seeded rule-create route regression (- CR-4b), Results widget blank cards with restricted Fields (- DX-2), ActionCell a11y labels, invisible empty macro parameter. An uncommitted host-side UIPage experiment on main broke the admin mid-pass (owner to keep-or-revert). |
| 2026-09-01 | SG-1 (PAUL 04-04) | **Merged** (b2d5234, approved first pass) — mixed suggestions, recent searches, no-results recovery. `SuggestMode.Mixed` (deterministic half-split with backfill, `Suggestion.group` emitted in every mode); contract +3 additive members (`group`, `didYouMean`, `popularSearches`); `RecoverySearchPipeline` decorator inside the cache, did-you-mean via `DirectSpellChecker` (new `Lucene.Net.Suggest` dep) with a `probe:true` verification search that never journals; popular searches = empty-prefix `IQuerySuggestionSource` reuse, opt-in (`PopularSearchesOnNoResults`, default 0); client-side recents (`localStorage`, cap 5) as a third panel group in both suggestion consumers, default on. Suites: Core 334, Admin 190, Ingestion 47, Widgets 78, JS 272 — lead re-verified. Neither STOP clause fired. | Checklist §S items 95–100 added; items 85–86 off KNOWN FAIL. Approved deviations: document picks not recorded as recents; panel opens on focus whenever a recent matches (not only empty input); PB Mode dropdown gained the Mixed option. Host follow-up (lead): bundle rebuilds + `SuggestMode.Mixed` / `PopularSearchesOnNoResults=5` on the demo index + restart. Future-unit candidates: phrase-aware correction, cross-tab recents sync, SSR recovery rendering. |
| 2026-09-02 | HL-1 (PF-1 follow-up defect) | **Merged** (cdbc7bb, approved first pass) — fuzzy-highlight cost fixed: `ExecuteSearchStage.PrepareHighlightQuery` rewrites `BaseQuery` against the reader ONCE per request inside the searcher lease (only when highlight fields requested and page non-empty), carried on new `SearchContext.HighlightQuery`; `LuceneHighlighter` scores `HighlightQuery ?? BaseQuery` (fallback keeps out-of-pipeline callers at today's behavior). No `IHighlighter` change — STOP clause never approached. Bench 3×100: fuzzy+highlight p50 135.18→8.19ms at 10k (p95 204→11), 136.8→11.4 at 100k, 166.9→35.4 at 1M; other matched rows unmoved; lead re-verified w/ independent cold smoke (11ms). Attribution double-proven: rewrite-only change collapses the row + new test asserts the prepared query holds concrete terms, no MultiTermQuery. Core 347 (+2). | Approved deviation: per-request Highlighter/QueryScorer reuse NOT done — measured as noise, and a stateful scorer shared across documents is behavioral risk for no gain (YAGNI, evidence in report). KNOWN-LIMITATIONS entry removed; performance-and-sizing + relevance-tuning updated to fixed numbers (135ms explicitly labeled a defect, not a property of fuzzy); 2026-09-01 artifact preserved, new dated -hl-1 artifact added. HighlightQuery set after cache-key computation, never projected — cache/response invariants test-pinned. |
| 2026-09-02 | PF-1 (PAUL 04-03, spec §12 perf) — **PHASE 4 COMPLETE** | **Merged** (ad7da9f, approved first pass) — `tests/XpSearch.Bench` (real `ISearchPipeline` stage chain, deterministic Zipf corpora 10k/100k/1M, uncached headline numbers, SP-1 measurement discipline, no new deps) + real-run artifact `docs/internal/perf-results-2026-09-01.md` + public guide `performance-and-sizing.md` incl. the §12-mandated honesty section (one writer, in-app search tier, shared-storage caveat, per-process freshness). Headlines: 10k = 2.8/3.7ms p50/p95 faceted+highlighted search; 100k = 5.0/8.3ms; 1M = 16.3/58.8ms, 1.24GB index, 189s build, deep-page ~95ms. Cache hit 0.00ms. Bench found a REAL defect, measured not fixed per spec: fuzzy+highlight p50 135ms (vs 4.85 fuzzy-no-highlight; ~170× exact-highlight cost; flat across sizes) — per-document Highlighter+QueryScorer re-expands the FuzzyQuery; KNOWN-LIMITATIONS entry w/ hoist-per-request upgrade path; filed as chip task_06905b09 (HL-1 candidate). Neither STOP clause fired; SearchTimingStage rider was already done (TM-1). Suites unchanged; SP-1 spike untouched. | No host/checklist items (nothing browser-verifiable). tests/ README corrected (phantom project rows). Committed run = representative (run 2 of 3; ~1.5× machine variance stated). Bench: Release-only, ~15min, ~1.5GB temp self-cleaned. Duplicate Kentico-free index seams (TestSearchIndex vs BenchIndex) noted — unify only if a third consumer appears. |
| 2026-09-02 | CD-1 (PAUL 03-01) | **Merged** (6c92033, approved first pass) — the 'command not found' defect DIAGNOSED, NOT a code defect: decompiled `Kentico.Xperience.Admin.Base` 31.8 discovery (`UITree.GetPageCommands` = `GetMethods(Instance\|Public)`, no `DeclaredOnly` — inherited + re-annotated-override commands ARE found once; duplicate names THROW at startup, never go missing; the miss is `node.Commands.TryGetValue` ordinal). Old convention ("[PageCommand] on abstract bases/overrides is suspect") was WRONG and is retired; also corrected: `ListingPage.Delete` carries NO attribute, so AD-8's overrides are required. Intermittency = stale host builds: the failing commands (AD-8 Delete merged 23:01:23, CR-5 SearchItems 22:23) did not exist in the assembly the host was running at ~23:00 on 2026-08-31 (ProjectReference + fixed 0.1.0 version). Shipped: `PageCommandDiscoveryTests` (70 cases through Kentico's REAL `UITree` singleton — every client-sent command resolves + assembly-wide uniqueness/registration guard; proven red on the pre-AD-8/CR-5 shapes), ADR-0027, primer correction. Admin 260 (+70). | Checklist §W 111–115 = owner click-through of the once-failing + never-clicked commands (host already runs a current build). No page reshaping — spec's §2 STOP honored (current shapes are correct; churn refused). Future: version-stamping unit before any NuGet handoff (fixed 0.1.0 guarantees stale package caches); platform landmines noted in ADR (SlugComparer inconsistency; GetUINode deepest-ancestor fallback = the other way a valid name can 404). |
| 2026-09-02 | EX-2 (PAUL 04-02, spec §10.7 example 2) | **Merged** (232a906, approved first pass; docs-only in the library — STOP clause held) — the computed-relevance-field worked example, built for real on the sample host: 30-day click counts from `IQueryLogStore` (one read per mapping scope) written as `clicks` via IX-1 `AddField`, `popular` sort key, host-owned `ClicksBoostStage` (`AddXpSearchStage`, tiers 2→1.5×/5→2.5×, no-op on sorted requests). Guide walkthrough in indexing-strategy.md opens with the built-in-vs-pattern framing (popularity-boosts.md cross-linked). Lead re-verified LIVE: sort=popular descends by clicks; boost moved a 5-click doc #6→#1 on a relevance query; explain line present; boost-is-not-a-filter and numeric-range checks pass. | Checklist §V 108–110. Host changes file-by-file (no git there); host RUNNING w/ rebuilt index. Built-in popularity toggle found OFF (owner's item-45 flow) and left OFF. Demo data: +4 seeded clicks on CleverDripper — owner re-running item 110 should pick a mid-list product. Same day: the flaky filterSort vitest case (chip task_ac2b2ed3, separate session) root-caused and fixed on main 7458bac — disposed `SearchClient` kept its retry loop; JS suite now 286 and stable across 5 runs. `/search` PB sort dropdown deliberately does not offer `popular` (owner decision pending). |
| 2026-09-01 | CL-1 (PAUL 04-01, spec §10.5) | **Merged** (fa5843d, approved first pass) — typed ingestion clients. New Kentico-free `XpSearch.Client` (NuGet `XperienceCommunity.Search.Client`, zero PackageReferences, pinned by KenticoFreeTests; 5th test suite, 16 tests) + npm subpath `@xperience-community/xperience-search/ingestion` (never on the root entry — API key is a server secret; package-check guards both directions). Owned verbs mirror the frozen routes; batching under real caps (1000 docs/10MB, upsert-only — deletes chunk by count), backoff+jitter on 408/429/5xx/transport w/ Retry-After, aggregate-so-far rides the exception (`PartialUpsert`). DTOs = second contract.mjs emission (3 lines; contract:check now 5 outputs). STOP clause not hit. | Checklist §U 104–107. **Lead host round trip PASSED both clients** (2026-09-01, live host): Node upsert→search(total 1)→clear('pim') drained w/ 32 xperience docs untouched; C# console app (project reference only) upsert→status→delete→listIndexes. Dev key plaintext recovered from CMS_EventLog (2026-08-31 seeder warning) — no reissue needed. Known: pre-existing FLAKY vitest case (filterSort probe/dispose, order-sensitive `calls[last]`) fails under load, passes isolated — task chip filed, recorded in primer; not CL-1's. Repo has NO solution file — suites run per-project (agent finding; a .sln is a future repo-wide change). |
| 2026-09-01 | IX-1 (PAUL 04-05) | **Merged** (7cddb43, approved first pass) — `XpSearchIndexingOptions.AddField` puts contributed fields in the index schema (after detected+flattened; detected wins collisions; exempt from Configure/Exclude — natural, `Apply` only runs in `FormInfoContentTypeFieldSource`); write-site guard warns once per undeclared field from `ContributeAsync`/`AddTaxonomyAsync` naming the AddField fix; `SuggestField` setter-tracked, one warning per index when Documents/Mixed suggests from the unconfigured `title` default. Core 345 (+11), other suites unchanged — lead re-verified. Neither STOP clause fired. Guide "Adding fields of your own" rewritten (was teaching the silent-invisibility trap). | Checklist §T items 101–103. Host adoption done same day (lead-direct): `DancingGoatSearchFieldSource` decorator deleted, three `AddField` calls in Program.cs (image+path on ProductPage, path on ArticlePage), README updated; verified live — path/image on the wire, event log clean, suggest unaffected. Approved deviations: duplicate AddField throws (spec left open); guard also covers AddTaxonomyAsync. Future: expose contributed fields on `IndexingContext.Fields`/a merged context schema if asked. |
| 2026-09-01 | DOC-1 (admin scope) | **Partial** — admin guide suite shipped to main (fb239c0); wiki sync blocked on wiki init | **Process:** docs-ship workflow live (skill + doc-specialist agent + Playwright screenshot pipeline, 22 captures + manifest). **Open:** wiki first-page init (owner), 2 variant captures, D1-D3 deferred until front-end settles. |
| 2026-09-02 | AR-1 (PAUL 06-01) | **Merged** (07928a7, approved first pass) - global search settings in the administration: one typed `XpSearch.Settings` row (16 global scalars of `XpSearchOptions`/`Analytics`) seeded once from the host lambda by the analytics installer, loaded over the lambda by `IConfigureOptions<XpSearchOptions>` (registered after `services.Configure`), refreshed live by an `IOptionsChangeTokenSource` on the row's insert/update events; all 12 src consumers moved to `IOptionsMonitor.CurrentValue`; **Settings** `ModelEditPage` under the Search ingestion app; `RetentionDays` default 180 -> 365; retention task now also prunes answered popularity/synonym suggestions (`DeleteAnsweredOlderThanAsync`, pending never) and reports three counts. Owner scope change mid-APPLY (rev 2 spec) after Kentico docs ruled out `SettingsKeyInfo` for custom settings. Core 357 (+10), Admin 261 (+1), Ingestion 47, Widgets 78, Client 16 - lead re-verified Core/Admin; full sln build 0 errors. | Checklist section X items 116-120 (+116a live update). Breaking (core): lambda globals used once, then admin-owned (delete `XpSearch_Settings` row to re-seed). KNOWN-LIMITATIONS: pruned answered suggestion can be re-suggested; upgrade-added columns read 0 and are repaired from code values (CacheTtl exempt). Host-only: seeding + change-token firing (no DB seam; Info objects need the Kentico container). Screenshot manifest: `ingestion--settings.png` STALE, never captured. |
| 2026-09-02 | AR-1 fix (owner walk items 116a/117) | **Merged** (76bef0e) - live update was delayed up to 30 min: the options rebuild read the settings row through an `IProgressiveCache` entry whose `ForInfoObjects<T>().All()` dependency did not invalidate on save. Cache layer removed (redundant: `IOptionsMonitor` caches until the change token fires; one `SELECT TOP 1` per save). Core 357/Admin 261; sln 0 errors. | Checklist 116a rewritten to probe the API (`pageSize` in the response) - the demo results widget sends its own page size. Items 116, 118, 119, 120 passed on the first walk; 116a/117 re-walk pending. |
| 2026-09-03 | AR-2 (PAUL 06-02) | **Merged** (9a11388, approved first pass) - search settings PER INDEX via named options: `XpSearchIndexSettings` resolved by `IOptionsMonitor.Get(registeredIndexName)` (root `XpSearchOptions` values = defaults for every index), `IConfigureNamedOptions` overlay reading the index row uncached, per-name invalidation via `IOptionsMonitorCache.TryRemove` on the row Insert/Update/Delete.After events, `ILuceneIndexAccessor.ResolveName/IndexNames` (ordinal names; only case can differ), pipeline resolves once into `SearchContext.IndexName`; one row per index (`SettingsIndexName`), no seeding, AR-1 global row deleted on install; retention + popularity per index, orphan indexes pruned with defaults; admin: `GlobalSettings` page deleted, `SearchSettingsPage` (IndexScopedEditPage) at Lucene Search -> index -> Search settings (slug search-settings, order 110). Core 360 (+3), Admin 262 (+1), Ingestion 47, Widgets 78, Client 16 - lead re-verified Core/Admin; full sln 0 errors. | Owner decision at the 06-01 checkpoint (per index, named options; multiple indexes = one cached instance per name). CHANGELOG Unreleased entries REWRITTEN (no global layer). Checklist section Y 121-125 (section X superseded). Merge conflict on the checklist resolved to main version. KNOWN-LIMITATIONS: orphan rows pruned with defaults; `<= 0` column = never written. Screenshot `index--search-settings.png` STALE. Popularity task reads the log once for the longest lookback, cuts per index. |
| 2026-09-03 | AR-2 fix (owner walk item 122) | **Merged** (583fb6b) - a settings save rebuilt the named options at once but the response cache kept the old page size for up to CacheTtl (a request without pageSize hashes to the same SearchCacheKey). `XpSearchIndexSettingsInvalidator` now also calls `ISearchCache.Evict(indexName)` on the row Insert/Update/Delete.After events. Core 361 (+1). Demo /search Results widget set to *Results per page* = 0 via the management API so the page follows the index setting (owner: the setting must drive the page). | Checklist 122 rewritten to verify on the demo page. UX-1 (tooltips + explanation text on every setting and widget property; label "Results per page (0 = index setting)") dispatched in parallel, spec `docs/internal/units/UX-1.md`. Owner question open: keep the widget page-size override (renamed) or remove it. |
| 2026-09-03 | UX-1 + AR-3 (PAUL 06-02 close-out) | **Merged** (4f6751f, approved first pass + 1 revise) - UX-1: tooltip + explanation text on every Search settings field and every widget property (12 property classes + shared base), guide wording + "Used by" column. AR-3 (owner): per-index Default page size / Default suggestion count REMOVED (code defaults stay for API callers, clamped by the index cap), Results / Search box / Suggestions sizes required min 1 (legacy 0 -> C# default, pinned), 0-branches deleted; Core installer now drops columns a form no longer declares (RuleStorageMigration precedent) - verified live: XpSearch_Settings lost both columns on start. Core 362, Widgets 80, Admin 262, Ingestion 47, Client 16; sln 0 errors. Rule: widgets own their sizes; the index owns the caps; API callers that send no size get the code default. | Checklist section Y 121-122 rewritten (cap test). Demo Results widget back to 6. Screenshot rows STALE (search settings, widget dialogs). Phase 7 (TH-6 artboard parity) running in parallel. |
| 2026-09-03 | PAUL Phase 6 (06-01 + 06-02) | **Yes** - owner walked checklist section Y 121-125 on the live host: per-index Search settings page (fourteen values, tooltips), a save is live on the first reload (cap 2 -> two cards; second index unaffected), retention per index pruned 619 query-log rows + 1 answered synonym suggestion with a 1-day window while the Test index kept 365, values survive restart, orphan indexes pruned with defaults. | 123 first read "Deleted 0" = the two "retention" labels were ambiguous (batch size edited instead of the window) -> unit/relabel micro-round in flight. Phase 7 (TH-6 artboard parity) in APPLY. |
| 2026-09-03 | relabel (Phase 6 follow-up) | **Merged** (009f3af, approved first pass) - "Retention: remove search analytics older than X days" / "Cleanup batch size (rows per delete)"; Search settings page grouped by [FormCategory] into Search / Suggestions / Analytics retention / Popularity boosts / Synonym suggestions (orders in tens; SearchSettingsCategoryTests pins the mapping). Admin 263. | Owner: item 121 re-look for the section headings; 123 reworded to the new label. |
