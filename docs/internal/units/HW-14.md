# HW-14 — Demo data parity: product images, path lines, clean + richer autocomplete

Host-only unit (like HW-12/HW-13): the host app is `F:\Personal\CommunityProjects\src` (NOT a
git repo; no commits — report file-by-file). The library at
`F:\Personal\CommunityProjects\libraries\xperience-search` is READ-ONLY reference; a capability
the demo needs but the library lacks is a REPORTED GAP, never a patch (standing rule). NEVER
touch `src\Components\Widgets\CardWidget\`.

Owner intent (2026-09-01): the demo must exercise the default card and autocomplete the way the
approved mockup shows them — product thumbnails, breadcrumb path lines, and an autocomplete
panel whose entries are real, labeled, and include recent searches — so the walkthrough docs
match what a visitor actually sees ([[feedback-default-is-the-design]]).

## 1. Index fields: `image` and `path`

Investigate how `DancingGoatSample` is registered (search `src/Search/` for the index/indexing
strategy registration; the library side is `XpSearchIndexingOptions`/strategy — read the
library's guides, e.g. indexing/field docs, for the supported mechanism):

1. **`image`** — populate from the product's image (Dancing Goat products carry an image
   asset/URL; find the attribute on `DancingGoat.ProductPage`'s model). The value must be a
   URL the site can serve. The default card renders `.xps-result__media` when `image` is
   present; the host's `productCard` in `src/Search/client/main.ts` and its Razor twin
   `src/Search/Products.cshtml` should ALSO render it (keep the pair in step — that is the
   HW-12 rule).
2. **`path`** — a breadcrumb-ish string (e.g. `Store › Grinders` from the page's tree path or
   category), indexed as a retrievable string attribute named `path` so the default card's
   path line renders with zero config (TH-1 reads `path` by default). Add it to the product
   card pair too, matching the mockup's muted line under the title.
3. Whatever fields list the Results widget/page uses must retrieve the new attributes (check
   the widget's Fields/FieldNames config on the live page — currently index defaults).

Index schema changes need a rebuild: find how the host rebuilds (admin UI is not available to
you — look for a programmatic route: the integration's rebuild API, an existing host endpoint,
or a small temporary dev endpoint you add and REMOVE after use; document exactly what you did).
You may stop/rebuild/restart the host (`Stop-Process -Name CommunityProjects`; `dotnet build
CommProjects.sln` from the umbrella root; `dotnet run --no-build --project
src/CommunityProjects.csproj` in background). The Vite bundle rebuild is `npm run build` in
`src/`.

## 2. Clean the suggestion source

The index still holds 3 external sample documents from ingestion smoke tests (titles like
`AeroPress-vf15ekn4`) that surface in autocomplete. Remove them: the ingestion API has a clear
route (`POST …/clear`; dev API key gotcha — the plaintext key is only readable at creation;
delete the `XpSearch_ApiKey` row and restart to reissue if you cannot find it — see
`docs/internal` notes and `src/Search/README.md`). Verify `/api/xpsearch/suggest` for `aero`
afterwards returns only real titles.

## 3. Autocomplete: labeled groups + recent searches

Target: the panel shows real document suggestions with group labels, plus recent/popular
searches, as the mockup's autocomplete state shows.

1. Investigate what the library ships TODAY: the suggest pipeline has document suggestions
   (prefix on the suggest field) and a query-suggestion service over the query log
   (`QuerySuggestionService` in Core/Analytics; the Suggestions widget has a `Mode`).
   Determine what `Mode` values exist and whether document + query suggestions can be served
   together (grouped), and whether the suggest response carries a group/type the widget's
   `groupLabels` maps.
2. Configure the demo to the RICHEST arrangement the library supports today without code
   changes to the library — e.g. suggest mode per index, `groupLabels` via the host bundle
   (the integrated search box takes `suggestions.groupLabels` — the host can wrap
   `registerWidgetType('searchBox', …)` the way it already wraps `results`/`activeFilters`),
   seeding a few realistic queries into the query log if query suggestions need history
   (issue real `/api/xpsearch/query` calls, e.g. "coffee", "grinder", "espresso" a few times).
3. Whatever the mockup shows that the library CANNOT do today (e.g. mixed document+query
   groups in one response, a "Recent searches" group, per-visitor recent searches) goes in the
   report as a precisely-described library gap with the evidence — that becomes the next
   library unit. Do not stub or fake it.

## 4. Verification

- `/api/xpsearch/query` for `q=grinder` returns products whose `image` and `path` attributes
  are populated (show one raw hit in the report).
- Live page: server first paint AND hydrated cards show thumbnail + path line for products
  (view-source + a node/jsdom check like HW-12's parity harness; you have no browser).
- `/api/xpsearch/suggest` for `aero` → only real titles; for a seeded prefix → whatever mode
  you configured, shown raw in the report.
- Both bundles rebuilt if you touched them; sln builds; host left RUNNING at the end.
- Append what you changed + browser checks (thumbnails, path lines, autocomplete groups) to
  the host-pass checklist (`docs/internal/host-pass-hw11-checklist-2026-08-26.md`, new section
  after the last) — the ONLY file you may write under `libraries/`.
- `src/Search/README.md` updated (new indexed fields, suggest configuration, the pair-of-cards
  rule now covering image+path).

## Constraints

- No library code changes (docs-append exception above). No new host dependencies. Temporary
  rebuild endpoint (if any) must be removed before you finish. Keep the two product cards
  (JS + Razor) byte-equivalent in what they render.
