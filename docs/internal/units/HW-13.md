# Unit HW-13 — Dancing Goat host: Vite ingestion of the npm client (host-only)

DISPATCH GATE: PK-1 AND PK-2 merged to main, and the library client rebuilt (`npm run build` in
`libraries/xperience-search/src/XpSearch.Widgets/Client`) so `dist/`, `scss/`, `styles/` exist.

Host-only unit like HW-12: the umbrella dir `F:\Personal\CommunityProjects` is NOT a git repo;
the host app is `src/` (Dancing Goat, :27340; search code under `src/Search/`). No commits —
report file-by-file what changed. NEVER touch `src/Components/Widgets/CardWidget/`.

Goal: make the sample host dogfood the npm-first path decided 2026-09-01 — the host builds its
own search bundle with Vite from `@xperience-community/xperience-search` (default theme +
first-party components), replacing the `<xps-search-assets />` tag helper on its layouts. Page
Builder widgets keep working, hydrated by the host bundle (the PK-1/PK-2 runtime-agnostic mount
contract, `docs/guides/server-rendering.md` in the library).

## Part 1 — Vite pipeline in the host

1. `src/package.json` (create; the host has none): dependency
   `"@xperience-community/xperience-search": "file:../libraries/xperience-search/src/XpSearch.Widgets/Client"`,
   devDependencies `vite` + `sass`. Scripts: `build` → `vite build`, `dev` → `vite build --watch`.
2. `src/vite.config.ts`: entry `src/Search/client/main.ts`, output to
   `wwwroot/dist/search/` with FIXED file names (`search.js`, `search.css` — no content hash;
   the layout references them statically), sourcemaps on, es2020 target.
3. Entry `src/Search/client/main.ts` + `main.scss`:
   - SCSS: `@use` the package's `scss/shell` and `scss/default` (or the per-widget partials for
     exactly the widgets the host mounts — prefer per-widget to prove granular ingestion; list
     which in your report).
   - JS: import `createSearch`/`mountAll` and ONLY the widget factories the Dancing Goat search
     pages actually mount (check the layouts/pages under `src/Search/` for which
     `data-xps-widget` types appear; import those via the `./widgets/<name>` subpaths). Register
     them explicitly per the PK-1 API — no UMD, no auto-registration.
   - Migrate `wwwroot/Scripts/xpsearchResults.js` (HW-12's custom results template override)
     into the entry as TypeScript using the typed `registerWidgetType` + `templates.item`
     imports; delete the old file and its `<script>` tag.
4. Bootstrap parity: whatever the UMD entry did to auto-mount on DOM ready, the host entry must
   do explicitly (call the documented mount/scan API). SSR handoff must keep working: server
   markup replaced on first render, server `QueryId` picked up so the hydration query isn't
   double-journaled — follow `docs/guides/server-rendering.md`.

## Part 2 — layout + host code swap

1. In the search layouts that currently emit `<xps-search-assets />` (find them — HW-12 touched
   both), replace the tag helper with `<link>`/`<script type="module" defer>` tags for
   `~/dist/search/search.css|js`. The tag helper must no longer render on those pages; the
   XpSearch.Widgets NuGet stays installed (Page Builder widgets + SSR come from it).
2. Fix host compile fallout from PK-2's namespace moves (e.g.
   `src/Search/SearchTemplateRegistration.cs` uses the result-template attribute — new
   namespace `XpSearch.Core.Rendering`).
3. `src/Search/README.md`: document the pipeline (npm install + build order: library client
   build → host `npm run build` → `dotnet build`), and that this host demonstrates the
   bundler-first setup while the tag helper remains the no-build alternative.

## Verification (no browser available to you)

- `npm run build` in `src/` succeeds; `search.js` contains the custom results template marker
  and does NOT contain a marker from an unimported heavy widget (proves granular imports held
  end-to-end).
- Solution builds (`dotnet build` the host sln).
- Static checks: no `<xps-search-assets`/`xpsearchResults.js` references remain in the search
  layouts; the two new asset tags are present.
- Append browser items to the current host-pass checklist doc (new section): first paint via
  SSR then hydration without console errors, custom product card renders, facets/pagination/
  URL routing work — i.e. the HW-11 §F/G behaviors, now under the host bundle; plus "view
  source shows no xpsearch.umd.js".

## Constraints

- No changes inside `libraries/` — if the npm package is missing something the host needs, STOP
  and report it as a PK gap; do not patch or fork the bundle (standing rule).
- Do not add other dependencies beyond vite + sass. Do not restructure host folders beyond the
  files named above. `node_modules`/`dist` hygiene: add a `src/.gitignore`-style note to the
  README instead (host isn't a git repo).
