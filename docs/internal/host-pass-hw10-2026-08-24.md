# Host pass HW-10 — 2026-08-24

Host: the Dancing Goat sample (`comm_projects`, :27340), owner-rebuilt against library `main`
6a7bac6 (+ the css-loader fix d82cf78 mid-pass). Run by the lead, driving the in-app browser with the
owner's administrator session; checklist = `host-pass-hw10-checklist-2026-08-23.md`. Written evidence
is DOM/API/DB-level; the owner should still eyeball anything visual.

## Verdict: PASS with 2 defects fixed during the pass, 3 logged, 2 items left to the owner's eye

## A. Redesigned admin pages
- **Analytics (1–4):** renders to the design with real data (108 searches, KPI tiles incl. 19.4 %
  zero-result rate, two-series chart). All four report tables paint fully inside Cards (no
  virtualization collapse — the top flagged risk). `Create rule` on the `yirgacheffe` row navigates
  to the seeded URL.
  **DEFECT (logged → CR-4b):** the seeded URL `/analytics/{seed}` renders the *dashboard*, not
  `ZeroResultRuleCreatePage` — even after a hard reload. Regression somewhere in the AD-4 wave
  (registration unchanged; SPA template resolution suspected). CR-4b replaces this flow; fix there.
  **DEFECT (logged, a11y):** the zero-result table's ActionCell buttons announce as "button" — they
  need an aria-label ("Create rule for {query}").
- **Query tester (8–9):** before-run state exact (Quick tip, placeholders, Run disabled with helper
  text); controls carry PZ-1's Contact group select.
- **Status (12–13):** initially **CRASHED** ("Something went wrong").
  **DEFECT FIXED (d82cf78):** css-loader 7 emits CSS modules as *named exports* by default, so the
  template's default `styles` import was `undefined` (`reading 'page'`). Fix: `modules: { auto: true,
  namedExport: false, exportLocalsConvention: "as-is" }`. After the fix the page renders to the
  design: Healthy tag, Documents 32 / Sources 1 / last external write "never", by-source bar + table
  (100 %), verbatim callouts, Recent ingestion in UTC. Rebuild → `Dialog` with the exact copy →
  success notification "Rebuild of 'DancingGoatSample' triggered." → header shows *Rebuilding* and
  the health card *Rebuild in progress · Started 2026-08-24 22:17 UTC* with the verbatim note.
- Not verified: 1b/1c/2c error states beyond unit evidence; 3b degraded (needs the owner-approved
  failing push); narrow variants (resize needs the pane visible); role-restricted items 7/16.

## B. Activities and contact groups
- **17:** activity-type descriptions refreshed, all four enabled. **18:** from a tracked session —
  query/no-results values are the query text; the click row carries the result id in
  `ActivityComment` and 1-based position in `ActivityItemDetailID`.
  **DEFECT FOUND + FIXED mid-pass (AN-4, merged 6a7bac6):** before the fix the click value was empty
  and cache hits logged nothing — `CachedSearchPipeline` re-issued queryIds the analytics map never
  saw. Verified after: repeated `chemex` search (cache hit) logged its own activity; the click row
  reads `chemex`.
- **19:** the three `XpSearchContact*` rules appear in the contact-group condition picker under
  **Web activity** with a working `{text}` parameter (`CMS_MacroRule` rows verified; parameter XML
  binds; note: an *empty* parameter renders invisibly until clicked — cosmetic, consider a watermark
  default). **20:** group "searched without results for text containing `yirgacheffe`" recalculated
  to 1 contact; a fresh matching search from another contact then joined **automatically** via
  activity-driven recalculation (2 contacts) — the ALL_ACTIVITIES fallback works. **21:** the system
  `…WithValue` rule untouched (usage location 4).

## C. Personalised rules (PZ-1) — the loop, end to end
- **22:** `XpSearch_Rule.RuleContactGroup` column added; Rules form shows the Contact group object
  selector after Enabled; listing has the column.
- **23:** rule "HW10 grinder boost" (Contains `coffee`, pin Hario Skerton → 1, group *Grinder
  shoppers* = `CoffeeGrinders`) saved through the real form.
- **24:** Query tester simulation (`contactGroup: CoffeeGrinders`): the grinder is injected at
  position 1, marked *Added by a rule*, boost line `rule:HW10 grinder boost (contact group
  CoffeeGrinders)`; as *Real visitor* (non-member): 0 changed. (The simulation was driven at the
  page-command level; the Select itself resists synthetic events but lists all three options.)
- **25:** live `/search` as the member contact: 25 results, grinder first. Declined-tracking half
  untestable — this host has no consent banner; tracking is on by default.
- **26:** anonymous API request inside the same cache TTL: 24 results, no grinder — the response
  cache keys on membership.
- Observation fixed as content: the Results widget's `Fields` config excluded `title`/`url`, so
  result cards rendered empty (the §5.8 server-template gap + no title-attribute override on the
  widget — logged as unit **DX-2**). Cleared `Fields` on the page so it paints (titles show the
  documented slug form).

## D. Page Builder
- **PB-4 in the real builder:** all nine widgets render `xps-editor-preview` blocks (badges, 32
  skeleton bars), **zero** `.xps-mount` elements, and the editor iframe fires **no**
  `/api/xpsearch/*` request. Live `/search` unchanged (nine mounts hydrate, no preview markup).
- **27/28 left to the owner's eye:** widget-list icons (classes verified against Kentico's list in
  HW-9) and the two attribute drop-downs' filtering in the configure dialog.

## Incidents during the pass
1. At 17:45 the lead's solution rebuild compiled an **uncommitted UIPage experiment** sitting on
   main's `src/XpSearch.Admin/UIPages/IndexTuning.cs` (tuning slug → "edit", "Edit index (xps)",
   reparenting) — the admin UI tree failed ("page with slug ':' is already registered"), and after
   the owner's 18:14 rebuild the old `/tuning/…` URLs 500 ("input string 'tuning' not in a correct
   format") while `/lucene/indexes/edit/2/…` works. The experiment is **not committed and not the
   CR-4a agent's work**; a backup + diff sit in the session scratchpad (`main-revert-backup/`).
   Owner to decide: keep (and unit-ify the rename) or revert.
2. The CR-4a implementer was wrongly suspected of the stray edit; its worktree copy of
   `IRelevanceTuningSource.cs` was briefly overwritten by the lead's recovery `cp` and re-applied.
   No agent edit ever landed on main.

## Defect ledger
| # | Severity | What | Disposition |
|---|---|---|---|
| 1 | high | Status page crash (css-loader named exports) | fixed, d82cf78 |
| 2 | high | analytics blind behind the cache / empty click values | fixed, AN-4 (6a7bac6) |
| 3 | medium | `/analytics/{seed}` renders the dashboard, not the seeded rule form | → CR-4b |
| 4 | medium | Results widget: `Fields` without title/url ⇒ blank cards; no title-attribute override; §5.8 templates render nothing | → DX-2 |
| 5 | low | ActionCell buttons unlabelled (a11y) | → CR-4b/AD follow-up |
| 6 | cosmetic | empty macro-rule `{text}` parameter is invisible until clicked | note |
